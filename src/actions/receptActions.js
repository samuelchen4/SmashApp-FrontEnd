import {
  GET_LOGIN_REQUEST,
  GET_LOGIN_SUCCESS,
  GET_LOGIN_FAIL,
} from '../constants/recept';

// can just call action types in the login page
// export const getReceptInfo = () => async (dispatch) => {
//   dispatch({type: GET_LOGIN_REQUEST});

// }
